# إصلاح ظهور الصفحة على GitHub Pages

سبب الصفحة البيضاء أن GitHub Pages يخدم ملف `index.html` من جذر المستودع، بينما هذا مشروع Vite/React يحتاج إلى خطوة بناء قبل النشر.

## الطريقة الموصى بها

1. ارفع المشروع كاملًا إلى GitHub، وليس الملفات العلوية فقط.
2. تأكد أن هذه المجلدات موجودة في المستودع:
   - `.github/workflows`
   - `src`
   - `public`
   - `tests`
3. من إعدادات المستودع افتح:
   `Settings > Pages`
4. في `Build and deployment` اختر:
   `Source: GitHub Actions`
5. ادفع أي تعديل إلى فرع `main` أو `master`.

بعدها سيبني GitHub التطبيق تلقائيًا وينشر مجلد `dist`.

## سبب عدم عمل الرفع اليدوي للملفات فقط

ملف `index.html` الموجود في جذر المشروع ليس النسخة النهائية للتصفح المباشر. هو مدخل يستخدمه Vite أثناء التطوير والبناء. النسخة التي تعمل على GitHub Pages تُنتج بعد تشغيل:

```bash
npm run build
```

وتكون داخل مجلد `dist`.
